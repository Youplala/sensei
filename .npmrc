registry=https://registry.npmjs.org/
# Don't set production mode as it excludes dev dependencies needed for build
# production=true
# Don't omit dev dependencies as they're needed for the build process
# omit=dev
# Reduce package size by removing unnecessary files
audit=false
fund=false
# Improve installation performance
prefer-offline=true
cache-min=3600